name: Resources
on: repository_dispatch
jobs:
  resources:
    name: Update resources
    runs-on: ubuntu-latest
    env:
      VERSION: ${{ github.event.action }}
    steps:
    - name: Read .nvmrc
      run: echo ::set-output name=nvmrc::$(cat .nvmrc)
      id: nvm
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v1
      with:
        node-version: '${{ steps.nvm.outputs.nvmrc }}'
    - name: Install dependencies
      run: npm install
    - name: Fetch resources
      run: node scripts/dump-connectors.js $VERSION
    - name: Update resources
      uses: alexesprit/action-update-file@master
      with:
        file-path: resources/connectors.json
        commit-msg: |
          Update resources

          Update connectors.json to match ${{ github.event.action }} version.
        github-token: ${{ secrets.GH_FA_TOKEN }}
